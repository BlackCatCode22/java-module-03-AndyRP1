public class Dog {
//Create two fields for our dog objects.

    String name;
    no usages
    int age;

    // Create a contstructor for the dog objects that will be created with the new keyword.
    // A constroctor is a special method that is called or invoked when... The Dog class is used
    // to create an object.
    public Dog()
        System.out.println("\n A dog obeject was created. This message is from the constructor\n");
}

//Create a constructor that accepts a string data type for the dogs name .
public Dog(String myName) {
    this.name = myName;
}


// Create a contructor that accepts an argument when an object is created, this argument will
//be an int
public Dog(int theAge)
    this.age = theAge;
Public Dog(int theAge)
      this.age = theAge;
    }
// Create a controctor that accepts two arguments - a string for the name , and an int or age
public Dog String()
        this.name = myName;























}
